# statistics are already with the Python package

from statistics import *

data = [12.5, 43.2, 5.2, 6, 23,6]
m = mean(data)
st = stdev(data)
data_min = min(data)
data_max = max(data)

print(m, st, data_min, data_max)

